# --- Cœur RAG ---------------------------------------------------------------
sentence-transformers>=3.0.0
transformers>=4.44.0
torch>=2.2.0
faiss-cpu>=1.8.0
accelerate>=0.33.0

# --- Quantization 4 bits (uniquement si vous utilisez Mistral-7B) -----------
bitsandbytes>=0.43.0

# --- Évaluation -------------------------------------------------------------
rouge-score>=0.1.2

# --- Utilitaires ------------------------------------------------------------
numpy>=1.26.0
pandas>=2.0.0
tqdm>=4.66.0

# --- (Optionnel) métriques RAG avancées : faithfulness, answer relevancy ----
# Nécessite un LLM juge (API ou local). Décommentez si vous l'utilisez.
# ragas>=0.1.0
# datasets>=2.20.0
